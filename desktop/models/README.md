# Local Model Cache

This directory is for local detector model files downloaded by:

```bash
npm run vision:model
```

`face_landmarker.task` is intentionally ignored and should not be committed.
The model can be re-downloaded during setup.
